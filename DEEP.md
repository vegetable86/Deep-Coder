<!-- deepcode:init:start -->
# DEEP.md

This file is generated by Deep Coder's `/init` command.
Use it as a concise project editing guide for this workspace.

## Project Map

- `README.md`: overview, setup steps, and contributor-facing usage notes.
- `deep_coder/`: authoritative implementation or active launch surface.
- `agentLoop.py`: authoritative implementation or active launch surface.
- `deepcode`: checked-in launcher or command entrypoint.
- `deep_coder/`: primary package or application code.
- `tests/`: automated regression coverage.

## Start Here

- `deepcode`: start the local launch flow here.
- `deep_coder/cli.py`: Python entrypoint behind `deepcode`.
- `deep_coder/cli.py`: command bootstrap inside the main package.
- `deep_coder/main.py`: composition root or runtime bootstrap.
- `README.md`: product overview and setup context before editing.

## Common Edits

- `deep_coder/context/`: session and context persistence
- `deep_coder/harness/`: runtime orchestration
- `deep_coder/models/`: model/provider adapters
- `deep_coder/projects/`: workspace and project registry logic
- `deep_coder/tools/`: tool schemas and local execution logic
- `deep_coder/tui/`: terminal UI and slash-command flow

## Verification

- `./.venv/bin/pytest -q`: repo-local pytest suite.
- `python3 -m pytest -q`: primary Python test suite.

## Boundaries

- `deep_coder/`: legacy or reference-only path; do not treat it as the active entrypoint.
- `agentLoop.py`: legacy or reference-only path; do not treat it as the active entrypoint.
- `~/.deepcode/`: runtime state or config lives outside the normal edit surface.
- `deep_coder/`: runtime state or config lives outside the normal edit surface.
<!-- deepcode:init:end -->

## Human Notes

Add your project-specific notes, conventions, and reminders below.
This section is preserved across `/init` refreshes.
